export type User = {
	id: string;
	name: string;
	image_url?: string;
};
export type Playlist = {
	id: string;
	name: string;
	image_url?: string;
};

export type Tracks = {
	id: string;
	name: string;
	image_url?: string;
	preview_url?: string;
};
